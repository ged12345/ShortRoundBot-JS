const { TREND_SHAPE } = require("../coin-bot/constants.js");

function calculateTrendShape(pointArr) {
    /* pointArr is of the 3 different gradients and the general assessment of these values */
}

module.exports = { calculateTrendShape };
